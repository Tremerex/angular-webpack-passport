# angular-webpack-passport
angular 1.6 + webpack + passport bearer strategy

Install dependencies:

npm install

Build production environment for webpack execute:

npm run build

Run the server:

npm run server
